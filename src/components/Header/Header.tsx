import React from 'react';
import { Navigation } from './Navigation';
import './Header.scss';

export const Header: React.FC = () => {
  return (
    <header>
      <Navigation />
    </header>
  );
};
